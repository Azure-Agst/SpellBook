// == CONFIG =========================

const BASEURL = "localhost";
const PORT = "8000";

// ===================================

const appUrl = BASEURL+":"+PORT

// ===================================

export { appUrl as default };
